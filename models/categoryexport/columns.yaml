# ===================================
#  List Column Definitions
# ===================================

columns:
    id: ID
    name: Name
    slug: Slug
    description: Description
    sort_order: Sort order
    enabled: Enabled
